# action.yml
name: 'udpPacketManager Build & Test'
author: 'David McKenna'
description: 'Build a container containing UPM and run the test suite.'
runs:
  using: 'docker'
  image: 'src/docker/Dockerfile'